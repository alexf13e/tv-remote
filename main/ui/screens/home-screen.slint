
import { RemoteButton } from "../custom-components.slint";
import { RemoteScreenID } from "../remote-screens-index.slint";
import { Button } from "std-widgets.slint";
import { Logic } from "../globals.slint";

export component HomeScreen {
    VerticalLayout { //container for whole screen
        spacing: 10px;

        RemoteButton {
            text: "TV Channels";
            screenID: RemoteScreenID.TV_CHANNELS;
        }

        RemoteButton {
            text: "TV Remote Buttons";
            screenID: RemoteScreenID.TV_ALL_BUTTONS;
        }

        RemoteButton {
            text: "Speakers All Buttons";
            screenID: RemoteScreenID.SPEAKERS_ALL_BUTTONS;
        }

        RemoteButton {
            text: "HDD All Buttons";
            screenID: RemoteScreenID.HDD_ALL_BUTTONS;
        }

        RemoteButton {
            text: "IR Code Learning";
            screenID: RemoteScreenID.IR_RECEIVE;
        }

        // Button {
        //     text: "sleep test";
        //     clicked => {
        //         Logic.sleep();
        //     }
        // }
    }
}