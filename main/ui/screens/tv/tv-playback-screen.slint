
import { Button } from "std-widgets.slint";

import { RemoteButton, TVRemoteNavPanel, TVPlaybackControlPanel } from "../../custom-components.slint";
import { RemoteScreenID } from "../../remote-screens-index.slint";


export component TVPlaybackScreen {
    VerticalLayout {
        spacing: 20px;
        
        VerticalLayout {
            alignment: center;
            vertical-stretch: 1;

            TVPlaybackControlPanel {
                height: 70%;
            }
        }

        RemoteButton {
            height: 40px;
            text: "Back to live TV";
            action_list_id: "TV_EXIT";
            screen_id: RemoteScreenID.TV_CHANNELS;
        }
        
        VerticalLayout {
            alignment: end;
            TVRemoteNavPanel {}
        }
    }
}