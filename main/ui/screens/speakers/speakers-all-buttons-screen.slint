
import { RemoteButtonDebugGrid } from "../../custom-components.slint";
import { SpeakersRemoteButtonData } from "../../custom-component-data.slint";

export component SpeakersAllButtonsScreen {
    RemoteButtonDebugGrid {
        grid_data: SpeakersRemoteButtonData.data;
    }
}